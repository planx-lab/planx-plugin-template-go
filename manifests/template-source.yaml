name: template-source
type: source
version: "0.1.0"
protocol: v4
description: "Template source plugin — replace with your connector details"
entry:
  binary: template-source
resources:
  max_sessions: 4
