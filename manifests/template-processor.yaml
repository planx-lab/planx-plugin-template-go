name: template-processor
type: processor
version: "0.1.0"
protocol: v4
description: "Template processor plugin — replace with your connector details"
entry:
  binary: template-processor
resources:
  max_sessions: 4
